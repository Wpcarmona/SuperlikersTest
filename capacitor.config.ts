import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wpcarmona.superlikersTest',
  appName: 'superlikertest',
  webDir: 'www'
};

export default config;
